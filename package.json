{
  "name": "react-nestjs-monorepo",
  "version": "1.0.0",
  "description": "monorepo with react and nestJS",
  "main": "index.js",
  "workspaces": ["server/", "client/"],
  "scripts": {
    "test": "npm test --workspace=client && npm test --workspace=server",
    "test:coverage": "npm test --workspace=client -- --coverage && npm test --workspace=server -- --coverage",
    "test:client": "npm test --workspace=client",
    "test:server": "npm test --workspace=server",
    "prisma:populate": "npm run prisma:populate --workspace=server"
  },
  "keywords": [
    "monorepo",
    "react",
    "node",
    "vite",
    "typescript",
    "ts",
    "boilerplate"
  ],
  "author": "Grimm",
  "license": "ISC"
}
